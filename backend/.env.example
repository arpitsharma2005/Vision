# Vision-Cast Backend Environment Configuration for Render
# Set these environment variables in your Render dashboard

# ===========================================
# APPLICATION SETTINGS
# ===========================================
NODE_ENV=production
PORT=5000

# ===========================================
# DATABASE CONFIGURATION
# ===========================================
# Use MongoDB Atlas - IMPORTANT: URL encode special characters in password
# If your password contains @, encode it as %40
# If your password contains #, encode it as %23
# Example: password@123 becomes password%40123
MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/vision-cast?retryWrites=true&w=majority&appName=Cluster0

# ===========================================
# AUTHENTICATION & SECURITY
# ===========================================
# Generate strong secrets using: openssl rand -base64 32
JWT_SECRET=your-super-secure-jwt-secret-key-change-this
JWT_REFRESH_SECRET=your-super-secure-refresh-token-secret-change-this

# ===========================================
# THIRD-PARTY API KEYS
# ===========================================
# Gemini AI API Key (required for image generation)
GEMINI_API_KEY=your-gemini-api-key

# Cloudinary Configuration (required for image storage)
CLOUDINARY_CLOUD_NAME=your-cloudinary-cloud-name
CLOUDINARY_API_KEY=your-cloudinary-api-key
CLOUDINARY_API_SECRET=your-cloudinary-api-secret

# ===========================================
# OPTIONAL SETTINGS
# ===========================================
# CORS Settings (set to your frontend domain)
CORS_ORIGIN=https://your-frontend-domain.com

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# ===========================================
# REDIS (OPTIONAL - for caching/sessions)
# ===========================================
# Only add if you plan to use Redis for caching
# REDIS_URL=redis://username:password@host:port